<h1><%= @user.username %></h1>
<h2><%= @user.email%> <p></h2>
</p>
<h3>Balance <%= @user.balance %> $</h3>
</p>

  <% @products.each do |product| %>
      <td><%= button_to "Add #{product}$", checkout_create_path, params: { id: product.id }, remote: true %></td><br>
  <% end %>
    